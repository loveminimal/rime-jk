# Rime dictionary - en_aliases.dict.yaml
# encoding: utf-8
# 
# --- 说明 ---
# 该字典是对既有英文词典的个人扩充
# 用于自定义字符串、命令的快捷输入
# 转化自个人使用的 .bash_aliases 脚本别名文件
# - https://github.com/loveminimal/rime-jk
# - Jack Liu <https://aituyaa.com>
# 
# 运行脚本：
# - https://github.com/loveminimal/rime-utils/blob/master/scripts/bash_alias2en_aliases_dict.py
# - py scripts/wubi86.py [-i src] [-o out] [-f file_endswith_filter] [-m multifile_out_mode]
#
---
name: en_aliases
version: '2025-01-10'
sort: by_weight
use_preset_vocabulary: false
...
ls -a	la	0
rm -rf	rr	0
vim	vv	0
vim ~/.vim/vimrc	vvc	0
emacs -nw	ee	0
vim ~/.bash_aliases	va	0
source ~/.bash_aliases	sa	0

# --- Ubuntu ---
sudo apt-get install	sai	0
sudo apt-get remove	sar	0
# Clear DNS
# sudo aptitude install nscd
sudo /etc/init.d/nscd restart	fdns	0

# --- Git ---
git status	gst	0
git add .	gaa	0
git commit -m	gcm	0
git commit --amend --no-edit	gca	0
git commit --amend	gcaa	0
git checkout	gco	0
git checkout	gcb	0
git checkout -b	gnb	0
git branch -d	grb	0
git branch	glb	0
git merge	gmg	0
git log --oneline --graph	glg	0
git log --oneline --graph -10	gll	0
git remote	gr	0
git remote -v	grl	0
git restore	grs	0
git pull	gl	0
git push	gt	0
git push github	gtg	0
git tag -l	gtl	0
git tag -n	gtn	0
git push --tags	gtt	0

# --- Python ---
winpty python	python	0
winpty python	py	0
pip3	pip	0
# --- NPM ---
npm install --registry=https://registry.npmmirror.com	npmi	0
npm --registry=https://registry.npmmirror.com	cnpm	0
pnpm --registry=https://registry.npmmirror.com	cpnpm	0
# === SSH =========
# Login remote server
ssh jack@175.178.60.98"     # Server aituyaa.co	ssss	0
ssh jack@aituyaa.lo"        # vm us6	ssvm	0


# Quickly access
# --- Windows ---
cd ~/AppData/Roaming/site	cds	0
cd ~/AppData/Roaming/site/themes/virgo	cdt	0
cd ~/AppData/Roaming/Rime	cdr	0
cd ~/AppData/Roaming/site/content/.obsidian/themes/Virgo	cdo	0
cd ~/AppData/Roaming/.emacs.d	cde	0
cd D:/sourcecode/sc_rime/rime-utils	cdu	0

# Blog
git add . && git commit -m 'perf: some updates' && git push	gpp	0
cd ~/AppData/Roaming/site && rm -rf public && hugo && source scripts/deploy.sh && rm -rf public	ssd	0
cd ~/AppData/Roaming/site && rm -rf public && hugo && scp -r public/* aituyaa.com:/home/jack/blog/ && rm -rf public	sscp	0

# Rime
git add . && git commit -m 'perf: dicts|symbols update' && git push	grr	0
cd ~/AppData/Roaming/Rime && py scripts/sync_wubi_user_dict.py	pysync	0

# Quickly access in OS like Unix
# --- Linux ---
cd ~/site	cdss	0
cd ~/site/themes/virgo	cdtt	0
cd ~/.emacs.d	cdee	0
# Blog
#alias serr="cd ~/site && live-server --open=public"
cd ~ && rm -rf blog && cd ~/site && rm -rf public && hugo && cp -r public ~/blog && rm -rf public && echo 'Deploy done.'	ssdd	0

# --- Hugo ---
hugo server -Dw	hs	0
hugo server -Dw -p 1314	hso	0

# --- Shell ---
cp ~/.shell/.bash_aliases ~/	cpsh	0
cp ~/.bash_aliases ~/.shell/	cphs	0






